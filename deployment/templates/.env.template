# VAST RAG Environment Configuration
# Copy this file to .env and customize for your environment

# Required: Source directory containing documents to index
# This should point to your project documentation, knowledge base, etc.
RAG_DOCS_PATH=$HOME/projects/RAG

# Required: Storage directory for RAG data (embeddings, indexes, cache)
# This will be created if it doesn't exist
RAG_DATA_PATH=$HOME/.claude/rag-data

# Chunking configuration
# Size of text chunks for embedding (in characters)
RAG_CHUNK_SIZE=500

# Overlap between chunks (in characters) to preserve context
RAG_CHUNK_OVERLAP=50

# Embedding model configuration
# Model from sentence-transformers (e.g., 'all-MiniLM-L6-v2', 'all-mpnet-base-v2')
RAG_EMBEDDING_MODEL=BAAI/bge-base-en-v1.5

# Batch size for embedding generation (adjust based on available memory)
RAG_BATCH_SIZE=32

# File watcher debounce interval (seconds)
# Prevents rapid re-indexing when files change quickly
RAG_DEBOUNCE_SECONDS=2.0
